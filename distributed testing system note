1.install the buildbot environment by following the tutorial.Specially pay attention to the python setting,you should set the python-dev.Using command line "sudo apt-get install python-dev".

2.Currently,the raw modle on the buildbot side is to Using the buildbot try scheduler,change the source code at local,adapt a strategy that decides which worker should pick up the change,build and test.The key part is to modify the master.cfg file.
